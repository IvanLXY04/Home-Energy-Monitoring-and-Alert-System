# Docker Backend (TIG Stack)
Run `docker-compose up -d` to spin up the entire data pipeline. 
Access InfluxDB at `YOUR_INFLUXDB_URL` and Grafana at `YOUR_GRAFANA_URL`.
Remember to generate an API token in InfluxDB and place it in `telegraf.conf` before starting.