# ESP32 Firmware
To flash this code:
1. Install the `Adafruit ADS1X15` and `PubSubClient` libraries via the Arduino IDE Library Manager.
2. Rename `config.h.example` to `config.h` and input your WiFi credentials.
3. Select "ESP32 Dev Module" and hit Upload.